node_modules/
dist/
.env
.env.save
*.mp3
*.mp4
.DS_Store

# Local-only YouTube OAuth token files — never commit
token-ai-doom-scroll.json
token-wet-circuit.json
token.json

# Render artifacts and scratch (large, regenerable, machine-specific)
tmp/
audio/
output/
video/
